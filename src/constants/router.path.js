const commentRouterPrefix = '/comment';
const fileRouterPrefix = '/upload';
const labelRouterPrefix = '/label';
const momentRouterPrefix = '/moment';
const userRouterPrefix = '/users';

const RouterPath = {
  commentRouterPrefix,
  fileRouterPrefix,
  labelRouterPrefix,
  momentRouterPrefix,
  userRouterPrefix
}
module.exports = RouterPath;